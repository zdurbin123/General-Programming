/*const subCollectionReducer = (state = [], action) => {
  if (action.type === 'ADD_COMIC') {
    return [...state, action.comic];
  }
  if (action.type === 'DELETE_COMIC') {
    copyState = [...state];
    index = copyState.findIndex((x) => x.id === action.id);
    copyState.splice(index, 1);
    return copyState; 
  };
};

export default subCollectionReducer;*/
